<form method="post" action="{{ path('product_inventory_delete', {'id': product_inventory.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_inventory.id) }}">
    <button class="btn">Delete</button>
</form>
